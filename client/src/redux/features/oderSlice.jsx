import { createSlice } from "@reduxjs/toolkit";

export const oderSlice = createSlice({
  name: "oder",
  initialState: [{}],

  reducers: {},
});

export const {} = oderSlice.actions;

export default oderSlice.reducer;
